# Never run dependency or package lifecycle scripts implicitly. Workflows and
# the Makefile invoke every required build step explicitly.
ignore-scripts=true

# Delay newly published dependencies long enough for ecosystem review.
# Requires npm 11.10 or later; the release toolchain pins Node.js 24.19.0
# (npm 11.17.0).
min-release-age=14
